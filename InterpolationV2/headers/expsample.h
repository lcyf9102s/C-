#ifndef EXPSAMPLE_H
#define EXPSAMPLE_H

class expsample
{
    public:
        double** beta_sa(double**, int, int);
        double** RSD_betaTH(double**, int, int);
        double** sample_Interpolation(double**, int, int);
};
#endif
